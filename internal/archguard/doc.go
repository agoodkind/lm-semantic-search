// Package archguard holds repo-wide architecture guard tests that enforce
// invariants no single package can express on its own, such as keeping the
// status breakdown vocabulary and its construction funnelled through one place.
package archguard

📁 {{ .Name }}
✅ Ready to search
{{- if .HasStats }}
📊 {{ .Files }} files, {{ .Chunks }} chunks
{{- end }}
{{- if .SkippedLine }}
{{ .SkippedLine }}
{{- end }}
{{- if .SyncNote }}
{{ .SyncNote }}
{{- end }}
🕐 Updated {{ .UpdatedAt }}

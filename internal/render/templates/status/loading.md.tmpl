📁 {{ .Name }}
◓ Semantic search for this codebase is not ready yet.
💡 Wait for indexing to complete with `lm-semantic-search codebase wait {{ .QuotedPath }}` or use the `wait_for_indexing` MCP tool.
{{ template "statusFooter" . }}

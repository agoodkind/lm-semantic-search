📁 {{ .Name }}
◓ Semantic search for this codebase is not ready yet.
💡 Search for this codebase resumes when this codebase is ready for semantic search.
{{- if or .GraphUpdatedAt .GraphReadyNoTime .GraphNotBuilt }}
{{ template "graphLine" . }}
{{- end }}
🕐 Updated {{ .UpdatedAt }}

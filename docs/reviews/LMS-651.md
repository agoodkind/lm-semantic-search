# LMS-651 adversarial review

LMS-651 is MERGE-READY through PR #260 review-fix head `5d8157a`, with all ten inline dispositions validated.

| Field | Value |
| --- | --- |
| Date | 2026-09-02 03:11 PDT |
| Branch | `codex/lms-651-retain-missing-watcher-paths` |
| Reviewed base | `f3601a4fe7821ac2900864a39d4ce2e19470b5e0` |
| Reviewed head | `5d8157ac60efaf642c15bede4383a4ba0b55b850` |
| Fix base | `17bda3b424b516a48a9d7194e2f6b18d84955a8f` |
| PR #260 fix base | `55689d47b48014cc19d1fb4a3f4e45473797dd54` |
| Live `origin/main` | `f3601a4fe7821ac2900864a39d4ce2e19470b5e0` |
| Class | Concurrency, lifetime, and user-visible progress |
| Reviewer tier | Strongest-model adversarial whole-branch review |
| Verdict | MERGE-READY |
| Catches | Prior review: 1 blocker, 1 should-fix, 0 nits. Fix re-reviews and PR #260: 0 blockers, 0 should-fixes, 0 nits. |
| Escapes | None found |

## Prior catches and disposition

1. **[BLOCKER, ADDRESSED] Slow present-path work repeated a running processed count.**

   The original review found that the rate limiter emitted after one second when `pathsSinceReport` was zero. A deterministic 256-path probe captured `256 of 256` before the first semantic write. After the first write took 1.1 seconds, the next running job version still reported `256 of 256`. Its `UpdatedAt` value advanced from `08:03:18.561026 UTC` to `08:03:19.663421 UTC`.

   The first fix suppressed every callback when the processed count had not advanced. PR #260 review correctly found that this also removed time-based heartbeats during slow present-path work. The current guard at [internal/daemon/converge.go:120](../../internal/daemon/converge.go#L120) permits an internal heartbeat callback. The watcher coordinator at [internal/daemon/background_sync.go:543](../../internal/daemon/background_sync.go#L543) converts the repeated count into a heartbeat-only update, so `UpdatedAt` and the progress journal do not change. The production regressions at [internal/daemon/converge_test.go:203](../../internal/daemon/converge_test.go#L203) were red at the PR fix base and green at `5d8157a`.

   **Disposition: ADDRESSED.**

2. **[SHOULD-FIX, ADDRESSED] Watcher comments promised deletion.**

   The original comments said an absent path was deleted and enqueued for removal. [internal/daemon/eventqueue.go:12](../../internal/daemon/eventqueue.go#L12) now describes semantic and checkpoint retention. [internal/daemon/watcher_test.go:206](../../internal/daemon/watcher_test.go#L206) now describes classification and retention. The old deletion phrases have no remaining match in the scoped source set.

   **Disposition: ADDRESSED.**

## Scoped fix-wave evidence

### Reproduce duplicate-progress red-green

```text
git restore --source=17bda3b424b516a48a9d7194e2f6b18d84955a8f -- internal/daemon/converge.go
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test ./internal/daemon -run '^TestConvergePathsDoesNotRepeatProgressAfterSlowPresentPath$' -count=1
git restore --source=HEAD -- internal/daemon/converge.go
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test ./internal/daemon -run '^TestConvergePathsDoesNotRepeatProgressAfterSlowPresentPath$' -count=1
```

The fix-base run exited 1 with `progress repeated after slow present path: 256 then 256`. The restored fix head exited 0. Only the reviewed production file changed during the red run, and the worktree was restored afterward.

### Reproduce progress and cancellation boundaries

```text
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -v ./internal/daemon -run '^TestConvergePaths(DoesNotRepeatProgressAfterSlowPresentPath|RateLimitsProgress|ReportsSlowClassificationProgress|ReportsFinalProgressAfterClassificationError|ReportsFinalProgressAfterClassificationCancellation|StopsBetweenPathsOnCancel|StopsBetweenPathsOnCancelWithoutProgress)$' -count=1
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -race ./internal/daemon -run '^TestConvergePaths(DoesNotRepeatProgressAfterSlowPresentPath|StopsBetweenPathsOnCancel|StopsBetweenPathsOnCancelWithoutProgress)$' -count=1
```

All seven focused tests ran and passed. All three focused race tests passed. Cancellation still stops before a second path with and without a progress callback.

### Sweep stale claims

```text
rg -n 'a path absent is deleted|delete-supersedes-reindex|converge can remove it|enqueue for removal' internal/daemon internal/semantic
```

The sweep exited 1 with no matches. Direct reads confirmed that both replacement claims match retained-path behavior.

### Rebuild and rerun the public boundary

The pre-review daemon identified itself as `17bda3b+dirty`, so it was not accepted as fix-head evidence. The three distribution executables were removed and rebuilt.

```text
rm -- dist/lm-semantic-search-daemon dist/lm-semantic-search dist/lm-semantic-search-mcp
make build
test dist/lm-semantic-search-daemon -nt /tmp/lms-651-rereview.LoObyU/before-build
test dist/lm-semantic-search -nt /tmp/lms-651-rereview.LoObyU/before-build
test dist/lm-semantic-search-mcp -nt /tmp/lms-651-rereview.LoObyU/before-build
dist/lm-semantic-search-daemon version
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -tags live -run '^TestWatcherRetainsRemovedPathAtPublicBoundary$' -count=1 ./test/live
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -count=1 ./internal/daemon
make check
```

The build and all freshness checks exited 0. The daemon reported committed revision `99f4158`. Built sandbox acceptance passed in 15.282 seconds. The full daemon suite passed in 35.032 seconds, and `make check` passed.

### Verify fix scope, signature, and merge reality

```text
git diff --name-status 17bda3b424b516a48a9d7194e2f6b18d84955a8f..99f415852ba6168ab1e23a70684ec68d0826adef
git diff --check 17bda3b424b516a48a9d7194e2f6b18d84955a8f..99f415852ba6168ab1e23a70684ec68d0826adef
git verify-commit 99f415852ba6168ab1e23a70684ec68d0826adef
git cat-file commit 99f415852ba6168ab1e23a70684ec68d0826adef
git fetch origin
git merge-tree --write-tree origin/main 99f415852ba6168ab1e23a70684ec68d0826adef
```

The fix changes only the progress guard, its regression test, and the two stale-comment sites. The diff check passed. The commit signature is valid, its raw object has `gpgsig`, and its Codex trailer is present. Live `origin/main` remained `f3601a4fe7821ac2900864a39d4ce2e19470b5e0`; the merge tree was clean.

No new fix-wave finding remains.

## PR #260 review-fix re-review

The PR review-fix wave is MERGE-READY. All ten inline dispositions match the current behavior, and the scoped diff introduces no new finding.

### Validate every inline disposition

| Comment | Disposition | Reproduced evidence |
| --- | --- | --- |
| `3912419167` | Invalid, upheld | Detached completion must use `PathsProcessed`. The all-missing test completed at 18,227 of 18,227 with zero converged paths. |
| `3912434269` | Valid, fixed | Slow present work advances `HeartbeatAt` without changing `UpdatedAt` or adding a second `job_progress` event. |
| `3912434397` | Valid, fixed | One successful path now persists in the checkpoint before a later path error propagates. |
| `3912437785` | Valid, fixed | The rate-limit test now injects a fixed clock, so wall time cannot inflate its callback count. |
| `3912440513` | Valid duplicate, fixed | The accumulated `changed` result covers this duplicate checkpoint-persistence comment. |
| `3912454635` | Valid, fixed | Owned Milvus cleanup registers before sandbox path and process startup. Normal built cleanup left the sandbox empty and preserved operator state. |
| `3912454795` | Valid, fixed | Duplicate comparison now includes `filesTotal`. Both equal-total rejection and changed-total allowance passed. |
| `3912454967` | Valid, fixed | Kill failure now returns immediately. Successful kill uses a second five-second bounded wait. The unsafe failure branch was reviewed statically. |
| `3912461137` | Valid, fixed | All three filesystem doubles now return real `os.FileInfo`; their disappearance, postclassification, and runner-error tests passed. |
| `3912464017` | Invalid, upheld | Detached watcher progress intentionally reports zero `FilesEmbedded`, so it cannot prove dependency recovery. The degraded dependency test remained degraded. |

### Reproduce heartbeat and checkpoint red-green

```text
git restore --source=55689d47b48014cc19d1fb4a3f4e45473797dd54 -- internal/daemon/converge.go
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -count=1 ./internal/daemon -run '^(TestConvergePathsReportsHeartbeatDuringSlowPresentPath|TestConvergeViaWatcherHeartbeatsSlowPresentPathWithoutDuplicateProgressJournal|TestConvergePathsPersistsSuccessfulPathBeforeLaterError)$'
git restore --source=HEAD -- internal/daemon/converge.go
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -count=1 ./internal/daemon -run '^(TestConvergePathsReportsHeartbeatDuringSlowPresentPath|TestConvergeViaWatcherHeartbeatsSlowPresentPathWithoutDuplicateProgressJournal|TestConvergePathsPersistsSuccessfulPathBeforeLaterError)$'
```

The fix-base run exited 1. It reported one heartbeat callback instead of two, an unchanged `HeartbeatAt`, and no checkpoint after the later error. The restored head exited 0 for all three tests.

A temporary review probe forced both a later path permission error and a checkpoint write error. The returned error matched the permission error and included the checkpoint write failure, so neither error was lost. The probe was deleted after it passed.

### Reproduce focused, race, and pushback checks

```text
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -v -count=1 ./internal/daemon -run '^(TestConvergePathsReportsHeartbeatDuringSlowPresentPath|TestConvergeViaWatcherHeartbeatsSlowPresentPathWithoutDuplicateProgressJournal|TestConvergePathsPersistsSuccessfulPathBeforeLaterError|TestConvergePathsRateLimitsProgress|TestConvergePathsRetainsPathRemovedAfterClassification|TestConvergePathsPropagatesPostclassificationLstatError|TestConvergePathsPropagatesIndexOneError|TestConvergeViaWatcherCompletesRetainedMissingProgress|TestCompletedWatcherConvergeKeepsDegradedDependency|TestConvergePathsStopsBetweenPathsOnCancel|TestConvergePathsStopsBetweenPathsOnCancelWithoutProgress)$'
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -race -count=1 ./internal/daemon -run '^(TestConvergePathsReportsHeartbeatDuringSlowPresentPath|TestConvergeViaWatcherHeartbeatsSlowPresentPathWithoutDuplicateProgressJournal|TestConvergePathsPersistsSuccessfulPathBeforeLaterError|TestConvergeViaWatcherCompletesRetainedMissingProgress|TestCompletedWatcherConvergeKeepsDegradedDependency)$'
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -v -tags live -count=1 ./test/live -run '^TestWatcherProgress(RejectsDuplicateRunningOneOfOne|AllowsTotalChange)$'
```

All eleven focused daemon tests passed. The five strongest cases passed under the race detector. Both live comparator tests ran and passed. These checks directly upheld both pushbacks.

### Rebuild and rerun isolation

```text
rm -- dist/lm-semantic-search-daemon dist/lm-semantic-search dist/lm-semantic-search-mcp
make build
test dist/lm-semantic-search-daemon -nt /tmp/lms-651-pr260-review.u3EKST/before-build
test dist/lm-semantic-search -nt /tmp/lms-651-pr260-review.u3EKST/before-build
test dist/lm-semantic-search-mcp -nt /tmp/lms-651-pr260-review.u3EKST/before-build
dist/lm-semantic-search-daemon version
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -v -tags live -count=1 ./test/live -run '^TestWatcherRetainsRemovedPathAtPublicBoundary$'
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -count=1 ./internal/daemon
make test
make check
```

The fresh build and all three modification-time checks exited 0. The daemon reported exact commit `5d8157a`. Built sandbox acceptance ran and passed, with an empty owned database after cleanup and no operator-state violation. The full daemon suite passed in 31.626 seconds. `make test` and `make check` passed.

### Verify signature and merge reality

```text
git diff --check 55689d47b48014cc19d1fb4a3f4e45473797dd54..5d8157ac60efaf642c15bede4383a4ba0b55b850
git verify-commit 5d8157ac60efaf642c15bede4383a4ba0b55b850
git cat-file commit 5d8157ac60efaf642c15bede4383a4ba0b55b850
git fetch origin
git merge-tree --write-tree origin/main 5d8157ac60efaf642c15bede4383a4ba0b55b850
```

The diff check passed. The fix commit has a valid signature, raw `gpgsig`, and the Codex trailer. Live `origin/main` remained `f3601a4fe7821ac2900864a39d4ce2e19470b5e0`, and the merge tree was clean.

No PR #260 review-fix finding remains.

## Original whole-branch evidence

### Verify merge reality

```text
git fetch origin
git rev-parse origin/main
git merge-base origin/main dd288d018149f07158bcdca6246f5907100fce28
git merge-tree --write-tree origin/main dd288d018149f07158bcdca6246f5907100fce28
```

All commands exited 0. `origin/main` and the merge base both resolved to `f3601a4fe7821ac2900864a39d4ce2e19470b5e0`. The merge tree resolved without conflicts as `1bfde54cd816d1705529902458c0dd2cc45a1717`.

No remote commit landed after the reviewed base. The recent-history overlap set was empty.

### Rebuild fresh executables

```text
rg --files --hidden --no-ignore -g '*.test' -g '!third_party/**' -g '!vendor/**'
go clean -testcache
rm -- dist/lm-semantic-search-daemon dist/lm-semantic-search dist/lm-semantic-search-mcp
make build
test dist/lm-semantic-search-daemon -nt /tmp/lms-651-review.OTXzNb/before-build
test dist/lm-semantic-search -nt /tmp/lms-651-review.OTXzNb/before-build
test dist/lm-semantic-search-mcp -nt /tmp/lms-651-review.OTXzNb/before-build
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -c -o /tmp/lms-651-review.OTXzNb/daemon.test ./internal/daemon
test /tmp/lms-651-review.OTXzNb/daemon.test -nt /tmp/lms-651-review.OTXzNb/before-test-build
test -x /tmp/lms-651-review.OTXzNb/daemon.test
```

No stale test executable existed. The three stale distribution executables were removed before the second build. `make build`, all freshness checks, and the fresh test compile exited 0.

The first direct test compile lacked the Make-provided `PKG_CONFIG_PATH` and exited 1. The canonical build provisioned the dependency. Repeating the compile with the exact Make exports succeeded.

### Run focused, full, and race gates

```text
/tmp/lms-651-review.OTXzNb/daemon.test -test.list '^(TestConvergePaths|TestClassifyConvergePaths|TestConvergeViaWatcher|TestCancelJobStopsWatcherConverge|TestCompletedWatcherConverge)'
/tmp/lms-651-review.OTXzNb/daemon.test -test.v -test.count=1 -test.run '^(TestConvergePaths|TestClassifyConvergePaths|TestConvergeViaWatcher|TestCancelJobStopsWatcherConverge|TestCompletedWatcherConverge)'
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -count=1 ./internal/daemon -run '^(TestConvergePaths|TestClassifyConvergePaths|TestConvergeViaWatcher|TestCancelJobStopsWatcherConverge|TestCompletedWatcherConverge)'
make test
make check
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -race -count=1 ./internal/daemon -run '^(TestConvergePaths|TestClassifyConvergePaths|TestConvergeViaWatcher|TestCancelJobStopsWatcherConverge|TestCompletedWatcherConverge)'
```

The fresh binary listed 25 matching tests and ran all 25. Every focused test passed. The direct focused run, `make test`, `make check`, and the focused race run exited 0.

The focused set covered missing classification, permission errors, postclassification disappearance, runner failure, cancellation, final progress, terminal totals, detached job state, and the 18,227-path case.

### Run deletion and present-path controls

```text
/tmp/lms-651-review.OTXzNb/daemon.test -test.v -test.count=1 -test.run '^(TestConvergeCopyChunksFiresOnRename|TestSyncIndexLargeRemovalQuarantinesBeforeDelete|TestHandleQuarantinedCodebase|TestAssessDeltaDeleteWave)'
```

All seven matching controls passed. Rename reuse remained active. Full-sync large-delete quarantine remained active after watcher-only quarantine deletion.

A temporary present-path probe forced semantic `Reindex` to return an error. The job still swallowed that error, reported zero converged paths, and wrote no checkpoint. LMS-651 did not expand the deferred present-path `Reindex` behavior.

### Reproduce missing-path red-green

```text
git switch --detach 2ea8e62239028b800fdbd204e81adf9a74b41db9
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -count=1 ./internal/daemon -run '^TestConvergePathsRetains18227MissingPaths$'
git switch codex/lms-651-retain-missing-watcher-paths
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -count=1 ./internal/daemon -run '^TestConvergePathsRetains18227MissingPaths$'
```

The red test commit referenced a counter that production had not added. Two counter-only assertions were temporarily removed so the old behavior could execute. Production source stayed unchanged. The red run exited 1 with `IndexOne calls = 18227, want 0`. The reviewed head then passed the same behavioral test.

### Attack progress and journal rate

Temporary review probes used production package boundaries and were deleted after execution.

The 256-present-path detached-job probe exited 1. It observed two running versions at `256 of 256`, separated by 1.1 seconds. This produced prior finding 1.

The 18,227-missing-path journal probe exited 0. It recorded one `job_progress` event, so count-driven callbacks did not flood the journal.

At reviewed head `dd288d0`, the detector-only live test passed:

```text
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -tags live -count=1 -run '^TestWatcherProgressRejectsDuplicateRunningOneOfOne$' ./test/live/
```

That green did not cover production progress, which required the later production-path regression.

### Run built sandbox acceptance

```text
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -tags live -count=1 -run '^TestWatcherRetainsRemovedPathAtPublicBoundary$' ./test/live/
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -tags live -count=1 -run '^TestWatcherRetainsRemovedPathAtPublicBoundary$' ./test/live/
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-651-retain-missing-watcher-paths/.make/cgo/darwin-arm64/lib/pkgconfig CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path go test -v -tags live -count=1 -run '^TestWatcherRetainsRemovedPathAtPublicBoundary$' ./test/live/
```

All three runs exited 0. The first two completed in 29.746 and 13.525 seconds. The verbose control showed an explicit test run and pass in 14.531 seconds.

Each run triggered two repeated watcher events. Every event preserved checkpoint bytes, checkpoint identity, row count, and vector hashes. Cleanup removed only source paths under the test directory, the owned sandbox root, tracked collections in the collision-checked temporary database, and that database.

### Verify every signature

Each command pair exited 0:

```text
git verify-commit COMMIT_SHA
git cat-file commit COMMIT_SHA
```

| Commit | `verify-commit` | Raw `gpgsig` |
| --- | --- | --- |
| `2ea8e62239028b800fdbd204e81adf9a74b41db9` | valid | present |
| `54eeea24f4548a4e093de5bc5482811071309851` | valid | present |
| `c0662b4d03c241ddd3193f5ae28478c142abe9d2` | valid | present |
| `7b54407d4ba8a270aba6fe25477cb232c297798c` | valid | present |
| `6a744f443d07541de3f2e678e5e1e3be57dafaf7` | valid | present |
| `15fad8201217fc5b72bfb51bed032054c0d5f937` | valid | present |
| `2cf5a55e1e1f4903f6e67435d3baabc25b5d27d5` | valid | present |
| `dd288d018149f07158bcdca6246f5907100fce28` | valid | present |

All eight signatures used the expected ED25519 signing identity.

## Evidence tiers

**Verified:** Every original, scoped, and PR #260 command, exit code, test count, red-green result, progress observation, journal count, sandbox result, signature, and merge result above was reproduced during review.

**Verified statically:** Only `os.ErrNotExist` classifies a path as missing. Other filesystem and runner errors fail. Detached heartbeats do not mutate progress versions, live totals, journals, or dependency health. Kill failure returns immediately, and successful kill has a bounded follow-up wait. The fix wave changes no scheduler admission or full-sync ownership behavior. Sandbox deletion targets are collision-checked resources created by the test.

**Inferred:** The clean merge tree has no semantic conflict because live `origin/main` still equals the reviewed base. No verdict claim depends only on this inference.

**Assumed:** None.

## Deliberately skipped checks

- Crafted external-byte attacks were skipped because this branch adds no parser or untrusted byte format.
- The complete opt-in live suite was skipped. The built watcher acceptance ran five times across all review rounds against local Milvus.
- The operating-system process-kill failure branch was not forced because doing so safely requires an uncontrollable process state. Its return and timeout bounds were verified statically.
- Installed service mutation and deployment were skipped because this review authorizes only the built sandbox boundary.
- Scheduler capacity and full-sync ownership integration were not mutated. Their unchanged scope was reviewed statically, and full-sync quarantine controls passed.

MERGE-READY

# LMS-650 adversarial whole-branch review

LMS-650 is MERGE-READY. The branch-only analyzer finding caught by the original
review is fixed, and the scoped re-review found no new issue.

## Review record

| Field | Value |
| --- | --- |
| Original review date | 2026-09-02 01:59:43 PDT |
| Scoped re-review date | 2026-09-02 02:32:00 PDT |
| PR 261 review-fix date | 2026-09-02 03:35:29 PDT |
| Branch | `codex/lms-650-bounded-reuse-reads` |
| Live base | `f3601a4fe7821ac2900864a39d4ce2e19470b5e0` |
| Original reviewed production head | `d712c44870b55a1b1f1abaf22dfeb5af6511c3ee` |
| Original review commit | `03009fabdaf3f4818ceb87b8df9e57b65304a3ee` |
| Fixed production head | `3e38c3ecd5de2d315414ab775777524576f81292` |
| PR 261 review-fix head | `ad83537adc063c8d06c35157a3c55d2265b25791` |
| Class | Milvus transport bounds, external response validation, and read-only reuse |
| Reviewer tier | Strongest-model whole-branch adversarial review |
| Verdict | MERGE-READY |
| Catches | B/SF/N: 1/0/0 |
| Escapes | 23 pre-existing `grpc_handler_missing_peer_enrichment` findings on `origin/main` |

## Original catch history

1. **[BLOCKER, ADDRESSED] The branch added one `grpc_handler_missing_peer_enrichment` finding.**

   At original review commit `03009fabdaf3f4818ceb87b8df9e57b65304a3ee`,
   fresh `origin/main` reported 23 findings. The branch reported the same 23
   plus `discoverStoredHashReuseCandidates` at pre-fix line 441. This made
   `make check` fail on a branch-introduced finding.

   Fix commit `3e38c3ecd5de2d315414ab775777524576f81292` extracts
   the incoming gRPC peer from `ctx` and includes its safe string form in the
   iterator-open error log at the
   [current implementation](../../internal/semantic/reuse_lookup.go#L442).
   The authoritative wrapped error remains unchanged.

   The scoped re-review reproduced 24 pre-fix analyzer findings and 23 fixed
   findings. `discoverStoredHashReuseCandidates` is the only removed identifier.
   The fixed 23-identifier set matches fetched `origin/main`. The blocker is
   addressed.

## Analyzer provenance

The original `make staticcheck-extra` run on fetched `origin/main` exited 2 with
23 findings. The original branch `make check` run exited 2 with 24 findings.
The original identifier set difference contained only
`discoverStoredHashReuseCandidates`.

The 23 shared findings are:

| Current source | Identifier and base line to branch line |
| --- | --- |
| [collection.go](../../internal/semantic/collection.go) | `addMissingConversationScalarColumns` 400 to 402 |
| [conversation_backfill.go](../../internal/semantic/conversation_backfill.go) | `BackfillConversationScalarColumns` 28 to 28; `conversationCollectionNeedsBackfill` 147 to 147; `BackfillConversationEnrichment` 430 to 430 |
| [conversation_batch.go](../../internal/semantic/conversation_batch.go) | `LoadConversationDerivedBatch` 46 to 46 |
| [conversation_search.go](../../internal/semantic/conversation_search.go) | `SearchConversationCollectionCapped` 22 to 22 |
| [conversation_state.go](../../internal/semantic/conversation_state.go) | `LoadConversationMessageState` 45 to 45 |
| [copy_chunks.go](../../internal/semantic/copy_chunks.go) | `CopyChunks` 32 to 32; `fetchChunksForPath` 215 to 215 |
| [health.go](../../internal/semantic/health.go) | `CollectionState` 68 to 68 |
| [mmap.go](../../internal/semantic/mmap.go) | `waitForCreatedMmapTargets` 308 to 308; `migrateMmapUnderMaintenance` 492 to 492 |
| [residency_controller.go](../../internal/semantic/residency_controller.go) | `AcquireCollection` 168 to 168 |
| [reuse.go](../../internal/semantic/reuse.go) | `LoadReuseVectors` 41 to 41; `LoadReuseVectorsForPrefix` 62 to 62; `LoadReuseVectorsForPath` 84 to 84 |
| [reuse_catalog.go](../../internal/semantic/reuse_catalog.go) | `getReuseCatalogModelKeys` 249 to 249; `queryReuseCatalogKeys` 310 to 310; `loadReuseCatalogRowKeys` 385 to 385 |
| [service.go](../../internal/semantic/service.go) | `searchCollection` 535 to 535; `collectionRowCount` 671 to 671 |
| [staging.go](../../internal/semantic/staging.go) | `PinStaging` 19 to 19; `embedMissedTexts` 608 to 608 |

The two-line shift in `collection.go` comes from naming existing schema limits.
It does not create another analyzer identifier.

## Scoped fix re-review

The fix range `03009fabdaf3f4818ceb87b8df9e57b65304a3ee..3e38c3ecd5de2d315414ab775777524576f81292`
adds three lines in one production file. It imports `google.golang.org/grpc/peer`,
reads the peer only on iterator-open failure, and adds one structured log field.
It changes no query, selected ID, vector, return value, write path, or success
path.

| Check | Result |
| --- | --- |
| Fix diff | `git diff --check` exited 0. One file changed with three insertions and no deletion. |
| Fix signature | `git verify-commit 3e38c3e...` exited 0. The raw commit contains `gpgsig` and the Codex trailer. |
| Nil peer safety | gRPC v1.83.2 `(*peer.Peer).String` returns `Peer<nil>` for a nil receiver. The background-context error test logged that value without panic. |
| Pre-fix analyzer | The isolated `03009fa...` checkout reported 24 `grpc_handler_missing_peer_enrichment` identifiers, including `discoverStoredHashReuseCandidates`. |
| Fixed analyzer | `GO_MK_SKIP_FETCH=1 make staticcheck-extra` reached analysis and reported exactly the 23 fetched-origin identifiers. It did not report `discoverStoredHashReuseCandidates`. |
| Fresh test binary | Built after epoch `1788341393`; mtime was `1788341407`; build exited 0. |
| Test selection | The fresh binary listed exactly `TestReuseReadErrorHasNoSideEffects` and `TestLoadReuseVectorsForContentsBoundsCandidateTransportResponse`. |
| Public read errors | Scalar Query and selected Get `Unavailable` errors passed through public `LoadReuseVectorsForContents`; both subtests passed in 43.58 seconds with zero embeddings and mutations. |
| Fake transport | The 65,536-byte public-caller transport test passed from the fresh binary. |
| Merge reality | `git merge-tree --write-tree origin/main HEAD` exited 0 with tree `0c0971143f33f02424526f9788919ecd76cac62d`. |
| New breakage inspection | The three-line diff is nil-safe, preserves `%w` wrapping, adds no concurrency or lifetime change, and leaves the transport path unchanged. |

The first canonical analyzer attempt did not reach analysis because bootstrap
could not resolve GitHub. The documented offline path,
`GO_MK_SKIP_FETCH=1 make staticcheck-extra`, used the cached verified engine and
produced the authoritative 23-finding result.

Exact scoped commands:

```sh
git diff --check 03009fabdaf3f4818ceb87b8df9e57b65304a3ee..3e38c3ecd5de2d315414ab775777524576f81292
git verify-commit 3e38c3ecd5de2d315414ab775777524576f81292
git cat-file commit 3e38c3ecd5de2d315414ab775777524576f81292

env GO_MK_SKIP_FETCH=1 make staticcheck-extra

rm -f -- /tmp/lms650-fix-review.test
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test -c -o /tmp/lms650-fix-review.test ./internal/semantic

/tmp/lms650-fix-review.test -test.list \
    '^Test(ReuseReadErrorHasNoSideEffects|LoadReuseVectorsForContentsBoundsCandidateTransportResponse)$'

/tmp/lms650-fix-review.test -test.v -test.count=1 \
    -test.run '^Test(ReuseReadErrorHasNoSideEffects|LoadReuseVectorsForContentsBoundsCandidateTransportResponse)$'

git merge-tree --write-tree origin/main HEAD
```

## Current findings

No open blocker, should-fix, or nit remains in the scoped fix or reviewed
whole branch. The original blocker remains above as addressed catch history.

## Original whole-branch reproduced checks

| Check | Result |
| --- | --- |
| Initial and final `git fetch --prune origin` | Exit 0. `origin/main` remained `f3601a4fe7821ac2900864a39d4ce2e19470b5e0`. |
| Initial and final `git merge-tree --write-tree origin/main HEAD` before the review commit | Exit 0. Clean merged tree `3f7720526bab697aa30bc3bb519df584fda9f9a7`. |
| `git diff --check origin/main...HEAD` | Exit 0. |
| Stale test executable search | No `*.test` file existed in the worktree. The two review executables under `/tmp` were removed before rebuilding. |
| Fresh semantic test binary | Built after epoch `1788337340`; mtime was `1788337358`; build exit 0. |
| Fresh live test binary | Built after epoch `1788337379`; mtime was `1788337391`; build exit 0. |
| `go clean -testcache` | Exit 0. |
| Focused fresh semantic binary | Exit 0. Fourteen top-level tests and sixteen subtests passed. |
| Temporary arithmetic attacks | Two review-only tests covered `MaxInt64` dimension overflow and near-`MaxInt64` packing. Both passed. The temporary file was removed, and the branch returned to a clean state. |
| `go test -count=1 ./internal/semantic` | Exit 0 in 149.282 seconds. |
| `make test` | Exit 0. The semantic package passed in 150.767 seconds. |
| `make check` | Exit 2. GolangCI-Lint, formatting, gocyclo, and deadcode passed. `staticcheck-extra` reported 24 findings. |
| Fetched `origin/main` `make staticcheck-extra` | Exit 2 with 23 findings. |
| Six production commit signatures | Every `git verify-commit` exited 0. Every raw commit object contained `gpgsig` and the Codex trailer. |

The temporary base checkout initially failed before test execution because its
unprovisioned cgo directory lacked `onnxruntime.pc`. The valid base run reused
the branch's provisioned cgo dependency directory and reached the test.

## Original exact command set

Head Go commands used the provisioned cgo directory through this prefix:

```sh
env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path
```

The executed commands were:

```sh
git fetch --prune origin
git merge-tree --write-tree origin/main HEAD
git diff --check origin/main...HEAD
find . -type f -name '*.test' -print
rm -f -- /tmp/lms650-semantic-review.test /tmp/lms650-live-review.test

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test -c -o /tmp/lms650-semantic-review.test ./internal/semantic

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test -c -tags=live -o /tmp/lms650-live-review.test ./test/live

/tmp/lms650-semantic-review.test -test.v -test.count=1 \
    -test.run '^Test(LoadReuseVectorsForContentsBoundsCandidateTransportResponse|ReuseVectorFetchBatchFitsExactBudget|ReuseVectorFetchBatchSplitsOneByteOverBudget|ReuseVectorFetchBatchNeverReturnsEmptyBatch|ReuseVectorFetchBatchesSelectedIDs|ReuseVectorFetchBatchRejectsOversizedRow|ReuseSelectedRowValidation|ReuseReadErrorHasNoSideEffects|ReuseScalarCandidateDiscoveryNeverRequestsVectors|ReuseModernCandidateDiscoverySelectsOneCompatibleID|ReuseLegacyCandidateDiscoveryQueriesOneContentAtATime|ReuseCandidateDiscoveryRejectsKnownUnequalModel|AcquireReuseSourceLoadsPrimaryButSkipsColdFallback|ColdFallbackReuseSkipsCollectionInspection)$'

go clean -testcache

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test -count=1 ./internal/semantic

make test
make check

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test -tags=live ./test/live \
    -run 'Test(DuplicateLegacyCorpusReuseImmutabilitySmoke|UntaggedReuseAcrossCorpusPreservesSourceRow|ReuseCatalogStoresEachKnownEmbeddingModel|CompleteCatalogHitSkipsCollectionFallback)' \
    -count=1 -v
```

The isolated base checkout ran:

```sh
make staticcheck-extra

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test ./internal/semantic \
    -run '^TestLoadReuseVectorsForContentsBoundsCandidateTransportResponse$' \
    -count=1 -v
```

Signature verification covered these exact commits:

```text
1d1277a5145a2f2bf564eaa061cd1c7435c232d9
0405a2e2b94cd26abeeb7e403ea8ad632f2b9529
8bb8448b4494f77647897307f24771ae70e8cfb2
0a27efa9a03feed74c5c7f4f4b20c4cf09060f33
6c711e33644199b0b40354c1ff7afe5e7f795461
d712c44870b55a1b1f1abaf22dfeb5af6511c3ee
```

For each commit, the review ran `git verify-commit COMMIT` and
`git cat-file commit COMMIT`.

## Fake transport red and green

The checked-in public-caller test ran against a real Milvus SDK client and a
fake gRPC server with a 65,536-byte send limit.

At `f3601a4fe7821ac2900864a39d4ce2e19470b5e0`, this command exited 1:

```sh
go test ./internal/semantic \
    -run '^TestLoadReuseVectorsForContentsBoundsCandidateTransportResponse$' \
    -count=1 -v
```

The old vector-bearing candidate response failed with:

```text
ResourceExhausted: grpc: trying to send message larger than max
(131662 vs. 65536)
```

At `d712c44870b55a1b1f1abaf22dfeb5af6511c3ee`, the same test exited 0 in
2.021 seconds. It selected `legacy-00`, fetched one exact 4,096-float vector,
and recorded zero embedding or mutation calls. This green result also proves
that one selected vector fits the same transport limit.

## Live preservation proof

The four-test live command exited 0 in 50.860 seconds. Filtered reruns recorded:

| Test | Result |
| --- | --- |
| `TestDuplicateLegacyCorpusReuseImmutabilitySmoke` | Pass in 19.55 seconds. Package exit 0 in 22.759 seconds. |
| `TestUntaggedReuseAcrossCorpusPreservesSourceRow` | Pass in 11.65 seconds. |
| `TestReuseCatalogStoresEachKnownEmbeddingModel` | Pass in 11.08 seconds. |
| `TestCompleteCatalogHitSkipsCollectionFallback` | Pass in 7.06 seconds. |

The smoke logged 16,384 duplicate rows, 4,096-float vectors, 16,387 source
rows, one catalog row, and duplicate vector SHA-256
`1361a0b47601e409bb624759fbb6c1f32a338468e645a062f9f0fa6edcc74c6c`.
Two lookups preserved every source scalar, every source vector hash, the full
catalog snapshot, and the source row count. The recorder observed zero data or
schema writes after setup, and the embedding call count did not change.

## Adversarial attack results

| Attack | Evidence | Result |
| --- | --- | --- |
| Duplicate amplification | 16,384 scalar candidates and the 65,536-byte fake transport | Candidate discovery requested no vector. The selected Get requested one ID. |
| Scalar query shape | The fake server rejects discovery queries that request `vector` | Modern discovery returned ID, content hash, and model identity. Legacy discovery queried one exact content and returned ID plus model identity. |
| Byte arithmetic | Exact budget, one byte over, zero dimension, oversized row, `MaxInt64` dimension, and near-`MaxInt64` budget tests | All expected passes and rejections occurred. No overflow or empty batch occurred. |
| 4,096-dimension estimate | 148,222 estimated bytes per row | A batch holds at most 452 rows. Its estimate is 66,996,344 bytes, below 67,108,864 bytes. |
| Deterministic IDs | Map keys are sorted before packing | The Get order was `first`, `second`. |
| SDK ID aliasing | Milvus SDK v2.6.5 mutates the VarChar slice while rendering `WithIDs`; production passes `slices.Clone(ids)` | The validator retained unquoted requested IDs and the selected Get tests passed. |
| Nullable model identity | Null, empty, equal, and unequal identities | Null, empty, and equal qualified. Known unequal IDs never reached Get. |
| Malformed selected rows | Missing column, unknown ID, duplicate ID, content collision, wrong model type, wrong vector type, wrong dimension, and incompatible identity | Every case returned an authoritative error with no accepted vector. |
| Query and Get errors | Injected `Unavailable` errors through public `LoadReuseVectorsForContents` | Both errors propagated. The embedder and mutation counters remained zero. |
| Cold fallbacks | Focused unit tests plus complete catalog live hit | Cold fallback inspection and warming remained absent. A complete catalog hit skipped the registry fallback. |
| Hidden writes | Fake mutation counters and live source plus catalog snapshots | No embedding fallback, catalog write, source write, delete, replacement, or backfill occurred. |
| Stale context | Repository search for removed helpers and vector-bearing candidate claims | No stale comment or document contradicted the new query shape. |
| Deletions | Whole diff and removed helper references | The old vector-bearing shared iterator was fully replaced. No removed defense or dead helper remained. |
| Threading and lifetime | Static review | The change adds no goroutine, lock, lease, or ownership transition. Query and Get use the caller context. |

## Controller rulings

- Local Milvus compacted 18 awaited flushes into seven loaded sealed segments.
  The local harness cannot reproduce the production topology of 81 sealed
  segments plus one growing segment.
- The deterministic public-caller fake transport test is the checked-in red and
  green proof.
- The 16,384-row live test is an immutability smoke only. This review makes no
  red-on-old claim for it.
- The true production transport proof belongs after deployment on the real
  Codex collection.
- LMS-648 remains outside this branch.

## Deliberately skipped checks

- I did not deploy or run the installed daemon against the real Codex
  collection. The controller assigned that proof to post-deployment acceptance.
- I did not repeat the 18-flush local topology attempt. The supplied run already
  proved compaction reduced it to seven sealed segments, so repetition cannot
  establish the missing 81-segment condition.
- I did not claim a live red state from the 16,384-row smoke. The producing lane
  proved that fixture passes on old code.
- The semantic search index status call timed out after 300 seconds. Git object
  reads, direct source searches, compiled tests, and live tests supplied the
  review evidence. The verdict does not depend on the semantic index.
- I performed no separate cancellation stress because the branch changes no
  concurrency or lifetime mechanism.

## PR 261 review-fix re-review

The review-fix range
`84e8067e295687ed4c2935955ff69a4d7e8e552e..ad83537adc063c8d06c35157a3c55d2265b25791`
adds one selected-response completeness check and its public regression. It
changes two files with 37 insertions and no deletion.

### Inline comment dispositions

| Comment | Disposition | Reproduced evidence |
| --- | --- | --- |
| `3912768822` | Invalid | gRPC v1.83.2 defines `(*peer.Peer).String`. The fresh semantic binary compiled and ran. |
| `3912768996` | Invalid | The approved design requires one exact legacy content per query. Batching multiple contents would violate that transport boundary. |
| `3912781062` | Valid and fixed | The pre-fix public regression returned nil error for an omitted selected ID. The fixed validator rejects every requested ID absent from `seen`. |
| `3912781111` | Invalid | Content can contain 65,535 bytes and reuse errors reach structured logs. The selected primary key identifies the malformed row without disclosing source text. |
| `3912781144` | Invalid | Model metadata can contain 65,535 bytes. The bounded selected-ID diagnostic identifies the rejected row without expanding the error payload. |
| `3912787103` | Invalid | gRPC v1.83.2 returns `Peer<nil>` from `(*peer.Peer).String` on a nil receiver. The background-context error test also passes. |
| `3912787187` | Invalid | Milvus client v2.6.5 `QueryIterator` exposes only `Next`. It has no `Close` operation. |
| `3912787319` | Invalid | The selected Get explicitly requests the model column. A present nullable column with a null row qualifies as unknown identity; an absent requested column is a malformed response. |

The eight-thread result is one valid and fixed comment plus seven invalid
comments. The implementer report's final sentence says “six invalid,” but its
eight detailed dispositions are complete. This is a report-only count typo,
not a branch finding.

### Reproduced fix evidence

| Check | Result |
| --- | --- |
| Fix diff and signature | `git diff --check` exited 0. `git verify-commit ad83537...` exited 0, and the raw commit contains `gpgsig` and the Codex trailer. |
| Missing-ID red | At `84e8067...` with the new test retained, the public regression exited 1 with `error = <nil>`. |
| Fresh binary | Built after epoch `1788344601`; mtime was `1788344647`; build exited 0. |
| Focused green | The fresh binary listed and passed eight top-level reuse tests and sixteen subtests. The new missing-ID test passed with nil reuse and zero embedding and mutation calls. |
| Partial response attack | A review-only two-ID test returned one selected row. The fixed public path rejected the omitted second ID, returned nil reuse, and recorded zero embedding and mutation calls. |
| Nullable versus absent model | The focused null-model subtest accepted a present null entry. A review-only public test removed the requested model column and received `ErrSearchResultIncomplete` with zero side effects. |
| Fake transport | The 65,536-byte public-caller transport test passed from the fresh binary. |
| Full semantic package | `go test -count=1 ./internal/semantic` exited 0 in 153.051 seconds. |
| Analyzer provenance | `GO_MK_SKIP_FETCH=1 make staticcheck-extra` reached analysis and reported exactly the 23 fetched-origin identifiers. Set comparison found no head-only or origin-only identifier. |
| Merge reality | After a final fetch, `git merge-tree --write-tree origin/main HEAD` exited 0 with tree `aa846539c9fd2549a4040fec25bcd3b1d8acdfd1`. |
| New breakage inspection | Validation finishes before `maps.Copy`. Deterministic requested IDs make the first omission stable. The fix adds no query, write, embedding, concurrency, or success-path change. |

Exact scoped commands:

```sh
git diff --check 84e8067e295687ed4c2935955ff69a4d7e8e552e..ad83537adc063c8d06c35157a3c55d2265b25791
git verify-commit ad83537adc063c8d06c35157a3c55d2265b25791
git cat-file commit ad83537adc063c8d06c35157a3c55d2265b25791

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test ./internal/semantic \
    -run '^TestReuseMissingSelectedIDHasNoSideEffects$' -count=1 -v

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test -c -o /tmp/lms650-pr261-review.test ./internal/semantic

/tmp/lms650-pr261-review.test -test.v -test.count=1 \
    -test.run '^Test(ReuseMissingSelectedIDHasNoSideEffects|ReuseSelectedRowValidation|ReuseReadErrorHasNoSideEffects|ReuseScalarCandidateDiscoveryNeverRequestsVectors|ReuseModernCandidateDiscoverySelectsOneCompatibleID|ReuseLegacyCandidateDiscoveryQueriesOneContentAtATime|ReuseCandidateDiscoveryRejectsKnownUnequalModel|LoadReuseVectorsForContentsBoundsCandidateTransportResponse)$'

env PKG_CONFIG_PATH=/Users/agoodkind/.worktrees/-Users-agoodkind-Sites-lm-semantic-search/lms-650-bounded-reuse-reads/.make/cgo/darwin-arm64/lib/pkgconfig \
    CGO_LDFLAGS_ALLOW=-Wl,-rpath,@loader_path \
    go test -count=1 ./internal/semantic

env GO_MK_SKIP_FETCH=1 make staticcheck-extra
git fetch --prune origin
git merge-tree --write-tree origin/main HEAD
```

This scoped pass did not rerun `make test` or the four live preservation tests.
The prior whole-branch review reproduced both. This fix changes only malformed
Get response validation and cannot enter a successful live lookup path.

## Evidence tiers

### Verified

- The live base, merge base, merge tree, changed files, and final fetched remote.
- PR 261's eight inline comments, with one valid fix and seven evidence-backed
  pushbacks.
- Missing selected IDs in empty and partial Get responses now fail before reuse
  is copied, with zero embedding or mutation calls.
- Present null model entries remain compatible, while an absent requested model
  column remains an authoritative malformed response.
- Fix commit `3e38c3ecd5de2d315414ab775777524576f81292`, its signature,
  its three-line diff, and its nil-peer behavior.
- The exact analyzer transition from 24 pre-fix identifiers to the 23
  fetched-origin identifiers, with no branch-only identifier remaining.
- Public Query and Get error propagation and the fake transport behavior after
  the fix, from a fresh test executable.
- Scalar discovery, exact legacy filters, deterministic selected Gets, model
  filtering, selected-row validation, and checked arithmetic.
- Fake transport red and green behavior.
- Source and catalog immutability in live Milvus.
- Full semantic tests, repository tests, partial check gates, analyzer
  provenance, signatures, and raw `gpgsig` headers.

### Inferred

- The 64 MiB estimator remains conservative for schema-valid selected rows.
  This review verified the estimate, not a 64 MiB serialized response boundary.
- The real Codex collection will exercise the production segment fan-in only
  after deployment.

### Assumed

- The supplied production topology has 81 sealed segments plus one growing
  segment.
- Local automatic compaction caused the observed reduction from 18 flushes to
  seven loaded sealed segments.

## Ledger row

No shared review log exists in this repository. This artifact is the durable
ledger row.

`2026-09-02 | codex/lms-650-bounded-reuse-reads | Milvus transport and read-only reuse | strongest-model adversarial | NOT-READY | B/SF/N 1/0/0 | escapes 23 pre-existing analyzer findings | one branch-only analyzer blocker`

`2026-09-02 | codex/lms-650-bounded-reuse-reads | scoped analyzer blocker fix | strongest-model adversarial | MERGE-READY | B/SF/N 0/0/0 | escapes 23 pre-existing analyzer findings | original blocker addressed by 3e38c3e`

`2026-09-02 | codex/lms-650-bounded-reuse-reads | PR 261 review-fix wave | strongest-model adversarial | MERGE-READY | B/SF/N 0/0/0 | escapes 23 pre-existing analyzer findings | one valid comment fixed, seven invalid comments verified`

MERGE-READY
